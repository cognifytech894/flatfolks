# Copy this file to .env.local (local dev) or .env (server) and fill in real values.
# Never commit .env.local or .env.

DB_HOST=localhost
DB_PORT=3306
DB_USER=flatfolks
DB_PASSWORD=
DB_NAME=flatfolks
