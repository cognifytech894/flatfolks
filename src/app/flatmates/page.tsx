"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { HeartHandshake, MapPin, Phone, Plus, UsersRound, UserRound, Wallet } from "lucide-react";
import { BackLink } from "@/components/ui/back-link";
import { lifestylePreferences } from "@/data/preferences";

type Requirement = { id: string; title: string; description?: string; location: string; rent: number; tags: string[]; listingKind?: string; ownerName?: string; contactPhone?: string; preferences?: string[]; genderPreference?: "Boy" | "Girl" | "Any" };

export default function FlatmatesPage() {
  const [requirements, setRequirements] = useState<Requirement[]>([]);
  const [loading, setLoading] = useState(true);
  useEffect(() => { fetch("/api/listings").then(async (response) => response.ok ? response.json() as Promise<Requirement[]> : []).then((listings) => setRequirements(listings.filter((listing) => listing.listingKind === "flat-requirement"))).catch(() => setRequirements([])).finally(() => setLoading(false)); }, []);

  return <main className="min-h-screen bg-slate-50 px-4 py-10 sm:px-6"><div className="mx-auto max-w-5xl"><BackLink label="Back to home" /><section className="mt-5 rounded-[2rem] border border-slate-200 bg-white p-6 shadow-sm sm:p-9"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-[.22em] text-emerald-600">Find Flatmates</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">People looking for a flat</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-600">These requirements are posted by people who need a flat. If you have a flat and need a flatmate, contact a suitable match.</p></div><Link href="/property?intent=flat" className="inline-flex shrink-0 items-center justify-center gap-2 rounded-xl bg-emerald-500 px-5 py-3 text-sm font-semibold text-white"><Plus className="h-4 w-4" />Add my requirement</Link></div><div className="mt-8 grid gap-4 md:grid-cols-2">{requirements.map((requirement) => <article key={requirement.id} className="rounded-2xl border border-slate-200 bg-slate-50 p-5"><div className="flex items-start justify-between gap-3"><h2 className="text-lg font-semibold text-slate-900">{requirement.title}</h2><span className="rounded-full bg-emerald-100 px-3 py-1 text-xs font-semibold text-emerald-700">Need a flat</span></div><p className="mt-4 flex items-center gap-2 text-sm text-slate-600"><UserRound className="h-4 w-4 text-slate-400" />Posted by {requirement.ownerName || "a FlatFolks member"}</p><p className="mt-2 flex items-center gap-2 text-sm text-slate-600"><MapPin className="h-4 w-4 text-slate-400" />{requirement.location}</p><p className="mt-2 flex items-center gap-2 text-sm font-medium text-slate-700"><Wallet className="h-4 w-4 text-slate-400" />Maximum budget: ₹{requirement.rent.toLocaleString("en-IN")}/month</p><p className="mt-2 flex items-center gap-2 text-sm text-slate-600"><UsersRound className="h-4 w-4 text-slate-400" />Looking for: {requirement.genderPreference || "Any"}</p>{requirement.description && <p className="mt-3 line-clamp-3 text-sm text-slate-600">{requirement.description}</p>}{requirement.tags.length > 0 && <div className="mt-4 flex flex-wrap gap-2">{requirement.tags.map((tag) => <span key={tag} className="rounded-full bg-white px-3 py-1 text-xs font-medium text-slate-600">{tag}</span>)}</div>}{requirement.preferences && requirement.preferences.length > 0 && <div className="mt-2 flex flex-wrap gap-2">{requirement.preferences.map((id) => { const preference = lifestylePreferences.find((item) => item.id === id); return preference ? <span key={id} className="rounded-full bg-emerald-50 px-3 py-1 text-xs font-medium text-emerald-700">{preference.icon} {preference.label}</span> : null; })}</div>}{requirement.contactPhone && <p className="mt-3 flex items-center gap-2 text-sm font-medium text-slate-700"><Phone className="h-4 w-4 text-slate-400" />{requirement.contactPhone}</p>}<Link href={`/interest?listingId=${requirement.id}`} className="mt-5 inline-flex rounded-lg border border-emerald-200 bg-white px-4 py-2 text-sm font-semibold text-emerald-700">I have a suitable flat</Link></article>)}{!loading && requirements.length === 0 && <div className="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-600 md:col-span-2"><HeartHandshake className="mx-auto h-8 w-8 text-slate-400" /><p className="mt-3">No flat requirements yet.</p><Link href="/property?intent=flat" className="mt-4 inline-block font-semibold text-blue-600">Be the first to add yours</Link></div>}{loading && <p className="py-10 text-center text-sm text-slate-500 md:col-span-2">Loading requirements…</p>}</div></section></div></main>;
}
